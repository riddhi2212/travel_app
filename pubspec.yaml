  name: travel_app
  description: Travel landing page mobile (responsive)
  version: 1.0.0+1

  environment:
    sdk: ">=2.18.0 <4.0.0"

  dependencies:
    flutter:
      sdk: flutter
    http: ^0.13.6
    provider: ^6.0.5
    flutter_svg: ^2.0.7
    cached_network_image: ^3.2.3
    intl: ^0.18.0
    flutter_rating_bar: ^4.0.1

  dev_dependencies:
    flutter_test:
      sdk: flutter

  flutter:
    uses-material-design: true

    assets:
      - assets/images/

